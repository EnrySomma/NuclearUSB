================================================================================
NUCLEARUSB OFFLINE KNOWLEDGE SYSTEM
CATEGORY: CODING
FILE: JavaScript_Reference.txt (JavaScript Quick Reference)
================================================================================

1. SCOPE AND CLOSURES
A closure is the combination of a function bundled together (enclosed) with
references to its surrounding state (the lexical environment). In JavaScript,
closures are created every time a function is created, at function creation time.
- Closures allow an inner function to access the scope of an outer function
  even after the outer function has executed and returned.
- Example:
  ```javascript
  function initMultiplier(factor) {
    // 'factor' is trapped in the closure lexical environment
    return function(number) {
      return number * factor;
    };
  }
  const double = initMultiplier(2);
  console.log(double(5)); // 10
  ```

2. ASYNC / AWAIT AND PROMISES
JavaScript is single-threaded and utilizes an event loop for asynchronous operations.
- Promises represent the eventual completion (or failure) of an async action.
  ```javascript
  const fetchData = () => {
    return new Promise((resolve, reject) => {
      setTimeout(() => resolve("Data loaded"), 1000);
    });
  };
  ```
- Async/Await provides syntactic sugar on top of Promises:
  ```javascript
  async function run() {
    try {
      const result = await fetchData();
      console.log(result);
    } catch (err) {
      console.error(err);
    }
  }
  ```

3. DOM MANIPULATION
Methods to interact with client browsers:
- Selectors: `document.getElementById('id')`, `document.querySelectorAll('.class')`.
- Event listeners:
  ```javascript
  const btn = document.getElementById('my-btn');
  btn.addEventListener('click', (event) => {
    event.preventDefault();
    console.log('Button clicked!');
  });
  ```
